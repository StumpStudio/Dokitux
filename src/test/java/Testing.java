import org.junit.Test;

public class Testing
{
    @Test
    public void testDownload() {
        /*Plugin plugin = PowerMockito.mock(Plugin.class);
        when(plugin.getDataFolder()).thenReturn(new File(Testing.class.getProtectionDomain().getCodeSource().getLocation().getPath()).getParentFile());
        when(plugin.getLogger()).thenReturn(Logger.getLogger("TestLogger"));
        PluginFileManager manager = new PluginFileManager(plugin);
        System.out.println(manager.findPlugin("PosePlugin").toString());
        manager.download(89050, new DownloadCallback() {
            @Override
            public void success() {
                System.out.println("SUCCESS");
            }

            @Override
            public void fail(String message) {
                System.out.println("FAILED TO DOWNLOAD FILE");
                System.out.println(message);
            }
        });*/
    }
}
